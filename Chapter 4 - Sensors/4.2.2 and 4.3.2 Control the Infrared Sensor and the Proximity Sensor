height = tello.get_distance_tof()

#height = the name of the variable
#height will store the height of the drone from ground by using this method “tello.get_distance_tof()”
#so you can use the height that you know to do something and this is the benefit of infared and proximity sensor
